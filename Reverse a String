翻转字符串

先把字符串转化成数组，再借助数组的reverse方法翻转数组顺序，最后把数组转化成字符串。

你的结果必须得是一个字符串

代码：
function reverseString(str) {
  // 请把你的代码写在这里
  var str_sep = str.split('');
  var rts = str_sep.reverse();
  var str_up = rts.join('');
  return str_up;
}

reverseString("hello");
